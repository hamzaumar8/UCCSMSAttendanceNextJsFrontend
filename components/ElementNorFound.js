const ElementNotFound = ({ children }) => {
    return (
        <div className="relative border border-primary bg-primary-accent rounded-lg w-full text-center p-5 space-y-4">
            {children}
        </div>
    );
};

export default ElementNotFound;
