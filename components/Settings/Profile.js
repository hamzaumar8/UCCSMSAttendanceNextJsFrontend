import Card from "../Card";

const Profile = () => {
    return (
        <Card header={<h1 className="text-xl font-extrabold">Profile</h1>}>
            <div className="space-y-3"></div>
        </Card>
    );
};

export default Profile;
